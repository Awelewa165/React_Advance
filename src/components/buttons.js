export const ReadMore = () => {
  return <button className="btn btn-primary">Read More</button>;
};

export const SignIn = () => {
  return <button className="btn btn-primary">Sign In</button>;
};

export const SignUp = () => {
  return <button className="btn btn-primary">Sign Up</button>;
};

export const Submit = () => {
  return <button className="btn btn-primary submitBtn">Submit</button>;
};
